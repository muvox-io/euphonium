#name: Build and Deploy web-ide
#on: [push]
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#
#      - name: Install and build
#        run: |
#          cd web-ide && yarn install && yarn build
#
#      - name: Deploy 🚀
#        uses: JamesIves/github-pages-deploy-action@v4.2.2
#        with:
#          branch: gh-pages
#          folder: web-ide/dist