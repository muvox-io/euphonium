import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.euphonium.app',
  appName: 'Euphonium App',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
